{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build Rust file",
            "command": "rustc",
            "args": [
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "${file}",
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ],
}
