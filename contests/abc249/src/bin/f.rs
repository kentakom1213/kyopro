#![allow(non_snake_case)]

use proconio::input;

fn main() {
    input! {
        N: usize,
        K: usize,
        TY: [(usize, isize); N]
    }

    // TYを後ろから見ていく
    // let mut q = 
}

const _INF: usize = 1001001001001001001;
