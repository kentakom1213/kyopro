#include <bits/stdc++.h>
using namespace std;

int main() {
  int N, M;
  cin >> N >> M;

  vector<pair<int, int>> relations;
  for (int i = 0; i < M; i++) {
    pair<int, int> relation;
    cin >> relation.first >> relation.second;
  }
}